import React from "react";

import { Layout } from "@/components/Layout";
import { Stats } from "@/components/Stats";

export const Home = () => {
  return (
    <Layout>
      <Stats />
    </Layout>
  );
};
