{
  "name": "onchain-signalling",
  "version": "1.0.0",
  "license": "MPL-2.0",
  "repository": "https://github.com/Granola-Team/onchain-signalling.git",
  "scripts": {
    "dev:client": "cd client && bun run build && bun run start",
    "build:client": "cd client && bun run build",
    "build:server": "cd server && cargo build",
    "install:client": "cd client && bun install",
    "install:server": "cd server && cargo check",
    "install": "yarn run install:client && yarn run install:server",
    "build": "yarn run install && yarn run build:client && yarn run build:server",
    "start": "yarn run install && yarn build:client && cd server && USER=$USER cargo run"
  }
}
