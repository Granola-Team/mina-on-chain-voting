type Query {
    blocks(limit: Int = 1, sortBy: BlockSortByInput): [Block]!
}

type Block {
    blockHeight: Int
    canonical: Boolean
    dateTime: String
}

enum BlockSortByInput {
    BLOCKHEIGHT_ASC
    BLOCKHEIGHT_DESC
}
